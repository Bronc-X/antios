# Workspace Rules

- This workspace is `main-only`.
- Before starting any task here, verify the current branch is `main`.
- Do not switch this workspace to `old10`.
- All rebuild, SwiftData, shell refactor, and `antios10` tasks must be handled only in this workspace.
- If a request targets `old10`, stop work here and redirect to `/Users/mac/Desktop/antios5`.
